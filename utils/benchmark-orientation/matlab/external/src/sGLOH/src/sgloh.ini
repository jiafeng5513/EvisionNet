// default settings

data:
{
	img="img.bmp"; // input image
	in="input.txt"; // input keypoints
	out="output.txt"; // output keypoints plus descriptors
	draw=FALSE; // show normalized feature patches if TRUE
	quantizer=512; // descriptor vector quantization levels if not 0
};

desc:
{
	dir=8;
	rad=[12,20];
	scale_factor=3;
	unique_center_bin=FALSE;
	hist_dir_cf=1;
};

dir:
{
	hist_std=0.7;
	weight_win=FALSE;
	smooth_std=1.0;
};

ort:
{
	adjust=FALSE;
	prec=8;
	dir=8;
	hist_std=0.7;
	weight_win=FALSE;
	smooth_std=1.0;
};


